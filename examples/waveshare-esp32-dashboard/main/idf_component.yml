## IDF Component Manager Manifest File
dependencies:
  ## Waveshare BSP — display (MIPI-DSI), touch (GT911), audio, etc.
  waveshare/esp32_p4_wifi6_touch_lcd_x:
    version: "*"

  ## LVGL graphics library
  lvgl/lvgl:
    version: "9.4.*"
    public: true

  ## WiFi via ESP32-C6 coprocessor (SDIO hosted)
  espressif/esp_wifi_remote:
    version: ">=0.14"

  espressif/esp_hosted:
    version: ">=1.4"

  ## MQTT client
  espressif/mqtt:
    version: "*"

  ## Required IDF version
  idf:
    version: ">=5.5.0"
